#!groovy

//@Library("Infrastructure")
@Library('Infrastructure@deployment-target-spike') 

def product = "plum"
def component = "frontend"

withPipeline("nodejs", product, component) {
  enableDockerBuild()
  //installCharts()
  after('checkout') {
    echo 'plum-frontend checked out'
  }
}
